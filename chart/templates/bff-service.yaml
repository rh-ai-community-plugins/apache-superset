{{- if .Values.bff.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "apache-superset.fullname" . }}-bff
  labels:
    {{- include "apache-superset.labels" . | nindent 4 }}
    app.kubernetes.io/component: bff
spec:
  type: {{ .Values.bff.service.type }}
  ports:
    - port: {{ .Values.bff.service.port }}
      targetPort: {{ .Values.bff.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "apache-superset.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: bff
{{- end }}
